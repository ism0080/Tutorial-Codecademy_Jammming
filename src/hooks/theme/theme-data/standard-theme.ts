export const standardTheme = {
  borderRadius: 8,
  colors: {
    defaultButton: 'green',
    defaultColor: '#fff',
    defaultDisabled: 'grey',
    defaultError: 'red',
  },
  defaultFontSize: 14,
}

export type ParaxisTheme = typeof standardTheme
